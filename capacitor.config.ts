import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'MaximoGatica_15844366-k',
  webDir: 'www'
};

export default config;
